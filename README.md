# Gulp + React + SCSS + hot-loading

Example repo to get the pipeline down

Uses the following:

Scripts:

* Gulp

Builders:

* Webpack
* Babel
* Hot Loading

View Layer:

* React
* Redux
* React Redux
* React Router
* React Router Redux
